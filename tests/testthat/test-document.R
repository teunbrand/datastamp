test_that("The correct document is reported", {
  # For some reason this works fine if the test file is tested but not when the
  # package is tested. Don't know how to fix this for now.
  
  # doc <- get_current_document()
  # expect_true(endsWith(doc, "tests/testthat/test-document.R"))
})
